name: 'tpch'
config-version: 2
version: '0.1'
profile: 'jaffle_shop'
model-paths: ["models"]
target-path: "target"
clean-targets: ["target", "dbt_modules", "logs"]
require-dbt-version: [">=1.0.0", "<2.0.0"]
models:
  tpch:
    +materialized: table
